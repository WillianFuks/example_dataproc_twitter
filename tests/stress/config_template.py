config = {"bucket": "bucket where intermediary dataproc data is located",
          "gcs_prefix": "prefix to retrieve blobs, such as folder/subfolder",
          "days_of_data": "how many days to load into locust testing"
         }

